# Changelog

## 0.1.0

- Initial MVP CLI with `init`, `run`, `collect`, and `report`.
- Local state file with command, git, environment allowlist, and file hash evidence.
- Markdown, JSON, and self-contained HTML dossier outputs.
- Default redaction for common secret patterns.
- Fixture-backed tests and smoke validation.
